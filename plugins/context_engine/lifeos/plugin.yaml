name: lifeos
description: LifeOS-aware context engine that routes project and people retrieval across Tony's separate LifeOS and WorkOS vaults while preserving the built-in compressor fallback.
version: 0.2.0
